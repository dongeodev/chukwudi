import styled from "styled-components"
import { ButtonSecundary } from "components/styleComponents/styleButtonSecundary"

export const Button = styled(ButtonSecundary)`
  border-top-left-radius: 8px;
  border-bottom-left-radius: 8px;
`
